# CLI Helpers

This directory is reserved for local operator tooling around the Canton Control Kit control plane.

Current contents:

- `canton/*.conf`: local topology templates for the multi-participant docker-compose setup

Recommended future additions:

- topology submission scripts
- party allocation helpers
- local demo bootstrap scripts
- receipt and audit exporters
