// Template participant config for participant2 in the local Canton Control Kit
// topology. Fill in the actual participant, ledger API, admin API, and domain
// connection settings for your environment.
